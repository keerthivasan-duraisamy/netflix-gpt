# Example environment variables for development/production
# Copy this to `.env` for local development or set env vars in Netlify
# Do NOT commit real tokens to git. Use Netlify Site > Settings > Build & deploy > Environment.

# TMDB API token (prefix required in CRA: REACT_APP_)
REACT_APP_TMDB_TOKEN=
